apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: cse
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: cse
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
