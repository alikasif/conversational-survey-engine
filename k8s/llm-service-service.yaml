apiVersion: v1
kind: Service
metadata:
  name: llm-service
  namespace: cse
  labels:
    app.kubernetes.io/name: llm-service
    app.kubernetes.io/part-of: cse
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: llm-service
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
